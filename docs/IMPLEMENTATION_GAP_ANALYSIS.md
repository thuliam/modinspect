# Implementation Gap Analysis

| Area | Current repo | Blueprint target | Gap | Priority | Action |
|---|---|---|---|---|---|
| Product Master | Local DB has >=10 active CPUs and >=10 active GPUs after POC migrations | Active canonical CPU/GPU catalog with reviewed facts | Facts are seeded fixtures, not fully reviewed master-data workflow | Medium | Add admin master-data write/review flow |
| Variant/Alias management | Tables exist; CPU/GPU POC products have >=5 aliases each; resolver DB test passes | Reviewed aliases/variants with resolver feedback | No create/edit/review actions | High | Add alias maintenance writes |
| Provider abstraction | `SearchProviderInterface`, mock provider, Gemini adapter, provider registry, deterministic query planner, and hard request guard implemented | Replaceable search/research/extraction adapters | Gemini live execution still blocked pending approval/API key | High | Run controlled Phase 2D-B live POC only after explicit approval |
| Offline evidence intake | `OfflineEvidenceImportService`, `OfflineFileProvider`, and `cli/import_market_evidence.php` import CSV/JSON records through candidate/evidence/extraction/resolution/validation/review | Real-world calibration before live API credentials, without bypassing review | CLI only; no browser upload, OCR, or bulk review UI | Medium | Use small reviewed REAL samples to calibrate before scale |
| Source policies | `data_sources` has policy fields; `source_incidents` records operational events; Admin source screen exposes health and pause/resume/enable/disable/incident actions | Explicit source policy, blocked/paused incident handling | No incident resolution UI yet | Medium | Add incident resolution workflow later |
| Collection jobs | `collector_jobs` is written by DB-backed mock CLI and Coverage Planner; mock scheduled runner consumes queued coverage jobs safely; failed jobs can be explicitly requeued | Coverage-planned jobs with budgets/idempotency | No installed external scheduler yet | Medium | Add Task Scheduler/deployment runbook later |
| Candidate store | `raw_price_observations` persists mock candidates idempotently | Candidate lifecycle states and dedupe | Status still coarse | Medium | Add candidate lifecycle service |
| Evidence | `market_evidence` persists private fixture evidence metadata and provider snippets where available | Traceable evidence records with levels/hashes | No direct page fetch/capture service | Medium | Add evidence fetcher after provider decision |
| AI extraction contract | Rule extractor writes `extraction_runs`; Gemini acquisition adapter maps search/research output to candidates | Versioned provider/model/schema output | No live AI execution approved yet | High | Keep dry-run until Phase 2D-B approval |
| Product resolver | Deterministic alias/name resolver now uses suffix-aware model markers and passes DB fixture/golden dataset tests | 95%+ accuracy on growing product set | Needs larger real reviewed dataset from live/community evidence later | Medium | Grow reviewed validation dataset after live POC |
| Validation | Generic validator plus CPU/GPU category rule packs integrated into mock DB pipeline with versioned reason codes and quality flags | Green/Amber/Red lanes with explainable category rules | Rules are deterministic but still fixture-calibrated, not production-calibrated | Medium | Calibrate against first reviewed live data |
| Review queue | Admin review queue lists pending records; approve/reject/exclude update status and audit logs; supported field-level corrections are auditable and validated | Approve/edit/reject workflow preserving corrections | No correction removal UI or batch review tooling yet | Medium | Add correction removal/batch tools later |
| Review analytics | `ReviewCalibrationService`, `cli/review_calibration_report.php`, and `/admin/review-analytics` report lifecycle, lane outcomes, clean-Green precision, corrections, flags, source/provider yield, and readiness with MOCK/REAL separation | Calibration dashboard for scaling review operations | No active hands-on review timer; no live sample yet | Medium | Reassess after first reviewed live POC sample |
| Accepted observations | Non-red mock records become pending observations; review can approve | Accepted observations from reviewed/green pipeline | No automatic promotion during calibration by design | Medium | Add review batch tools |
| Price Engine | Deal Checker range evaluator plus deterministic snapshot service with formula/cohort/confidence versions, calculation hash, manifest, snapshot membership, reviewed-value corrections, and confidence-v1 component breakdown | Deterministic snapshots from accepted observations with explainable confidence | Formula is still v1 and category-specific weighting remains basic | Medium | Add category-specific pricing policies later |
| Price snapshots/history | `price_indices`, `price_histories`, and `price_snapshot_observations` preserve new snapshot artifacts, included/excluded observations, and confidence-v1 snapshot-time inputs | Reproducible rolling snapshots with confidence audit | Legacy snapshots have no historical membership/confidence provenance by design | Low | Keep legacy rows marked provenance unavailable |
| Source health | Deterministic CLI/Admin reports calculate attempted/succeeded/failed jobs, yield, lanes, duplicate rate, last success/failure, consecutive failures, and healthy/degraded/paused/unknown state | Health metrics, pause/block, budgets | No incident resolution UI yet | Medium | Add incident resolution workflow later |
| Quality metrics | Offline evaluator reports CPU/GPU resolution accuracy, classification accuracy, Green coverage/precision, false-Green rate, wrong-nearby false-match rate, and rule triggers on 101 golden fixtures | Resolver/extractor/invalid-listing metrics | No broad reviewed production-quality dataset yet | Medium | Add reviewed real-world cases after live POC |
| Auditability | Review decisions write `audit_logs` | Broad audit trail for admin/review/data changes | Other admin actions not audited yet | Medium | Extract audit service and wire future writes |
| Stability/integrity checks | Phase 2D-J adds duplicate review-state protection, mock-source snapshot exclusion, atomic claim regression, and `cli/data_integrity_check.php` | Routine QA and integrity monitoring | Plain PHP DB-heavy tests still share one local database and should run sequentially unless isolated | Medium | Add isolated test databases before parallel CI |
| Asking vs sold representation | `price_observations.price_value` stores neutral price value; `asking_price` is nullable for non-asking rows | Strict asking/sold separation | No sold-price-specific snapshot UI yet | Medium | Add sold-price review/reporting once real sold evidence exists |
| Public UI | Home, price, detail, deal, methodology, compare, build, seller advisor, market report, seller, shop, and local-match routes audited; unsupported actions are disabled | Read-only MVP with transparent limits | Public MVP still shallow because many products have no accepted snapshots | Medium | Add reviewed observations and snapshot provenance |
| UX/UI design foundation | Phase 4A created `docs/UX_UI_AUDIT.md`, `docs/DESIGN_SYSTEM.md`, and `docs/USER_FLOWS.md`; low-risk branding, terminology, and offline asset dependency fixes are applied | Public UX should answer product/price/confidence/trust/next action quickly; Admin UX should prioritize attention-required work | Code-level audit only; owner visual review is still required before full redesign | Medium | Owner reviews Home, Price Index, Product Detail, Deal Checker, Admin Dashboard, and Review Queue |
| Community Data Feedback Loop | Approved architecture in Blueprint v1.1 and ADR-005 only | Users submit evidence/signals for price, product, spec, stale data, invalid listings, and sold-price reports | Not implemented; no schema/routes/UI/services by design | Future | Phase 4.5 after Public Read-Only MVP |
| Admin security | Admin routes require local login and static RBAC; server-side permissions protect review/source/job actions; actor identity reaches audits | Hardened production admin access | No MFA/SSO/password reset, external security review, or deployment hardening yet | High before public deployment | Keep Admin private until TLS/server hardening and security review |
| External spend controls | MODINSPECT ZERO-COST POC POLICY is documented in `AGENTS.md`, `docs/PROJECT_STATUS.md`, and `context/architecture_rules.yaml` | No paid/metered/billing-required service can be enabled without explicit owner approval | Enforcement is currently a durable engineering rule/config policy, not a runtime billing-control service | High | Stop with `OWNER_APPROVAL_REQUIRED` before any future cost-incurring milestone |
| Seller | Seller dashboard and advisor are labeled partial; advisor gates on accepted snapshots | Seller submissions and evidence profiles | No seller accounts/submissions | Low | Defer until data POC works |
| Local Match | Page is explicitly disabled with no fake nearby results | Optional approximate-location matching | No location/source/consent backend | Low | Defer |
| B2B | Shop inventory read-only demo uses DB rows and honest empty states | Shop feed/import, aging, demand analytics | No inventory write/filter workflow | Low | Defer large dashboard/API |
